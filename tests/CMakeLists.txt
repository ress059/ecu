if(${ECU_ENABLE_BUILD_TESTING})
    add_subdirectory(build)
elseif(${ECU_ENABLE_UNIT_TESTING})
    if(${MCU} STREQUAL "linux")
        add_subdirectory(unit)
    else()
        message(FATAL_ERROR "Unit tests can only run on Linux target.")
    endif()
endif()
