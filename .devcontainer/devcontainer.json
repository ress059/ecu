{
	"name": "ecu",
	"dockerComposeFile": "${localWorkspaceFolder}/compose.yaml",
	"service": "devcontainer",
	"workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
	"customizations": 
	{
		"vscode": 
		{
			// VSCode extensions to install when container is first created.
			"extensions": 
			[
				"cschlosser.doxdocgen",
				"ms-vscode.cpptools-extension-pack",
				"marus25.cortex-debug",
                "mcu-debug.debug-tracker-vscode"
			], 
			"settings": 
			{
				"C_Cpp.default.configurationProvider": "ms-vscode.cmake-tools",
				"C_Cpp.autoAddFileAssociations": false,
                "cmake.buildBeforeRun": true,
                "cortex-debug.enableTelemetry": false,
                "cortex-debug.variableUseNaturalFormat": false,
				"debug.hideSlowPreLaunchWarning": true,
				"editor.detectIndentation": false,
				"editor.insertSpaces": true,
				"editor.tabSize": 4,
                "telemetry.enableTelemetry": false,
                "telemetry.telemetryLevel": "off",
				"terminal.integrated.defaultProfile.linux": "bash",
				"workbench.editor.enablePreview": false,
                "[restructuredtext]": {
                    "editor.wordWrap": "on"
                }
			}
		}
	},
    "mounts": 
    [
        "type=bind,source=${localEnv:HOME}${localEnv:USERPROFILE}/.ssh,target=/root/.ssh,readonly"
    ],
    "postCreateCommand": "git config --global core.autocrlf input" // Avoids CRLF vs LF from showing up in git diff.
}