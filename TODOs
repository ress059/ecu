dll:

1. In circular_dll_ctor()
initialize id and destroy to:

list->head.id = RESERVED_ID;
list->head.destroy = function defined in .c file that does ASSERT(false);

2. Test compilation error if ecu_event_signal and ecu_circular_dll_node_id declared as unsigned

3. Finish object ID and event scheme. Add object ID scheme to dll.

4. Define ecu_event_init(struct ecu_event *me, ecu_event_id id_0); function??