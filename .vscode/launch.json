{
    "version": "0.2.0",
    "configurations": 
    [
        {
            "name": "Native Debug - GDB",
            "cwd": "${workspaceFolder}",
            "program": "${command:cmake.launchTargetPath}",
            "type": "cppdbg",
            "MIMode": "gdb",
            "request": "launch",
            "stopAtEntry": true
        }
    ]
}